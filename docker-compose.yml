services:
  agent:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env.local
    environment:
      - CONFIG_PATH=/app/config.json
    volumes:
      - ./config.json:/app/config.json:ro
    restart: unless-stopped
